-- Configuration
function love.conf(t)
    t.title = "Scrolling Shooter Tutorial" -- title of the window the game lives in
    t.version = "0.10.1"
    t.window.width = 480
    t.window.height = 800

    -- For win debugging
    t.console = true 
end
