-- Configuration
function love.conf(t)
    t.title = "Magus" -- title of the window the game lives in
    t.version = "0.10.1"
    t.window.width = 800
    t.window.height = 608

    -- For win debugging
    t.console = true
end
